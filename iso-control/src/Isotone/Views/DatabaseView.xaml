<UserControl x:Class="Isotone.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{DynamicResource PrimaryBackgroundBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Database Management" 
                   FontSize="32" 
                   FontWeight="Light"
                   Foreground="{DynamicResource PrimaryTextBrush}"
                   Margin="0,0,0,24"/>

        <!-- Content -->
        <materialDesign:Card Grid.Row="1" Padding="16">
            <StackPanel>
                <TextBlock Text="MariaDB Status" 
                         FontSize="20" FontWeight="Medium"
                         Margin="0,0,0,16"/>
                
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Database" Margin="0,0,8,0"/>
                        <TextBlock Text="Open phpMyAdmin"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="DatabaseEdit" Margin="0,0,8,0"/>
                        <TextBlock Text="Configure MariaDB"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>