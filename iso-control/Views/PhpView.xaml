<UserControl x:Class="Isotone.Views.PhpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#0F1829">
    
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="PHP Management" 
                               FontSize="28" 
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Manage PHP versions and extensions" 
                               FontSize="14"
                               Foreground="#8B92A7"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Command="{Binding RefreshVersionsCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#1E2A3A"
                        BorderBrush="#2A3C54"
                        Foreground="White"
                        Height="40"
                        Padding="16,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" 
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Warning Banner (if Apache is running) -->
            <Border Grid.Row="1" 
                    Background="#3D2800"
                    BorderBrush="#FFA500"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16"
                    Margin="0,0,0,16"
                    Visibility="{Binding IsApacheRunning, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AlertCircle" 
                                           Width="24" 
                                           Height="24"
                                           Foreground="#FFA500"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                    <TextBlock Text="Apache is running. Changing PHP version will restart Apache automatically."
                               Foreground="#FFA500"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="400"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Version Selection -->
                <Border Grid.Column="0"
                        Background="#1A2332"
                        CornerRadius="12"
                        Padding="24"
                        Margin="0,0,12,0">
                    <StackPanel>
                        <!-- PHP Version Selection -->
                        <TextBlock Text="PHP Version" 
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,12"/>

                        <ComboBox ItemsSource="{Binding AvailableVersions}"
                                  SelectedItem="{Binding SelectedVersion}"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  Background="#0F1829"
                                  Foreground="White"
                                  Margin="0,0,0,24"/>

                        <!-- PHP Info -->
                        <TextBlock Text="Version Information" 
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,12"/>

                        <Border Background="#0F1829"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,0,0,16">
                            <TextBlock Text="{Binding CurrentPhpInfo}"
                                       Foreground="#8B92A7"
                                       FontFamily="Consolas"
                                       FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <!-- Quick Actions -->
                        <TextBlock Text="Quick Actions" 
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,8,0,12"/>

                        <Button Command="{Binding OpenPhpFolderCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                BorderBrush="#2A3C54"
                                Foreground="White"
                                Margin="0,0,0,8"
                                HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FolderOpen" 
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="Open PHP Folder" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding OpenPhpIniCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                BorderBrush="#2A3C54"
                                Foreground="White"
                                HorizontalContentAlignment="Left"
                                Margin="0,0,0,24">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileDocument" 
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="Edit php.ini" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Apply Changes Button -->
                        <Button Command="{Binding ApplyChangesCommand}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="#4A9EFF"
                                BorderThickness="0"
                                Foreground="White"
                                Height="48"
                                FontSize="14"
                                FontWeight="SemiBold"
                                HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="Check" 
                                                       Width="20"
                                                       Height="20"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="Apply Changes" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Right Panel: Extensions Management -->
                <Border Grid.Column="1"
                        Background="#1A2332"
                        CornerRadius="12"
                        Padding="24"
                        Margin="12,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Extensions Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="PHP Extensions" 
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       VerticalAlignment="Center"/>

                            <Button Grid.Column="1"
                                    Command="{Binding SelectAllExtensionsCommand}"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="#4A9EFF"
                                    Margin="0,0,8,0">
                                <TextBlock Text="Select All"/>
                            </Button>

                            <Button Grid.Column="2"
                                    Command="{Binding DeselectAllExtensionsCommand}"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="#4A9EFF">
                                <TextBlock Text="Deselect All"/>
                            </Button>
                        </Grid>

                        <!-- Extensions List (Compact) -->
                        <ScrollViewer Grid.Row="1"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding AvailableExtensions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#0F1829"
                                                CornerRadius="6"
                                                Padding="12,8"
                                                Margin="0,0,0,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <CheckBox Grid.Column="0"
                                                          IsChecked="{Binding IsEnabled}"
                                                          VerticalAlignment="Center"
                                                          Margin="0,0,12,0"
                                                          Style="{StaticResource MaterialDesignCheckBox}"/>

                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock FontWeight="SemiBold"
                                                               Foreground="White"
                                                               FontSize="13">
                                                        <Run Text="{Binding DisplayName}"/>
                                                        <Run Text=" - " Foreground="#6B7280"/>
                                                        <Run Text="{Binding Description}" Foreground="#8B92A7" FontWeight="Normal" FontSize="12"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

        </Grid>
        </ScrollViewer>
        
        <!-- Loading Overlay (Full Page) -->
        <Border Background="#DD000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="0"
                             IsIndeterminate="True"
                             Width="60"
                             Height="60"
                             Foreground="#4A9EFF"/>
                <TextBlock Text="Processing..."
                           Foreground="White"
                           FontSize="16"
                           FontWeight="Medium"
                           Margin="0,20,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
